@model List<RentedMovieOneValid.Models.Movies>
@{
    ViewBag.Title = "Index_Movie";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Html.ActionLink("Add New Movie","Create","Movies")
<h2>Movie Details</h2>
<table class="table table-bordered table-hover" >
    <tr>
        @*<td>Movie_id</td>*@
        <td>Movie_Name</td>
        <td>Genre</td>
    </tr>
    <tbody>
        @foreach(var b in Model)
     {
            <tr>
                <td>
                    @Html.ActionLink(b.MovieName, "Movie_Details", "Movies", new { id = b.id }, null)
                </td>
                <td>
                    @b.Genre.GenreName
                </td>
            </tr>
        }
    </tbody>
</table>
